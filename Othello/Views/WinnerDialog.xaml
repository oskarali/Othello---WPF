<Window x:Class="Othello.Views.WinnerDialog"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          Title="Game Over - Winner!" 
          SizeToContent="WidthAndHeight" 
          WindowStartupLocation="CenterScreen"
          ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Winner message -->
        <TextBlock Grid.Row="0" 
                     Name="txtWinnerMessage" 
                     FontSize="20" 
                     FontWeight="Bold" 
                     HorizontalAlignment="Center"
                     Margin="0,0,0,20">
              Winner: [Name]
        </TextBlock>

        <!-- Score display -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock FontSize="16">Black:</TextBlock>
            <TextBlock Name="txtBlackScore" FontSize="16" FontWeight="Bold" Margin="5,0,20,0">0</TextBlock>
            <TextBlock FontSize="16">White:</TextBlock>
            <TextBlock Name="txtWhiteScore" FontSize="16" FontWeight="Bold" Margin="5,0,0,0">0</TextBlock>
        </StackPanel>

        <!-- Congratulations message -->
        <TextBlock Grid.Row="2" 
                     Text="Congratulations!" 
                     FontSize="14" 
                     HorizontalAlignment="Center"
                     Margin="0,10,0,20" />

        <!-- Close button -->
        <Button Grid.Row="3" 
                  Content="Close" 
                  IsDefault="True"
                  IsCancel="True"
                  MinWidth="100" 
                  HorizontalAlignment="Center" />
    </Grid>
</Window>
