<Window x:Class="Othello.Views.SetupGameDialog"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          Title="New Game Setup" 
          SizeToContent="WidthAndHeight" 
          WindowStartupLocation="CenterScreen"
          ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <!--Player 1 -->
        <Label Grid.Row="0" Grid.Column="0" Content="Player 1 Name:" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Name="txtPlayer1Name" Margin="5" Text="Player 1" />

        <Label Grid.Row="1" Grid.Column="0" Content="Player 1 Type:" VerticalAlignment="Center" />
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <RadioButton Name="rbPlayer1Human" Content="Human" IsChecked="True" Margin="0,0,20,0" />
            <RadioButton Name="rbPlayer1Computer" Content="Computer" />
        </StackPanel>

        <!-- Player 2 -->
        <Label Grid.Row="2" Grid.Column="0" Content="Player 2 Name:" VerticalAlignment="Center" Margin="0,10,0,0" />
        <TextBox Grid.Row="2" Grid.Column="1" Name="txtPlayer2Name" Margin="5,15,5,5" Text="Player 2" />

        <Label Grid.Row="3" Grid.Column="0" Content="Player 2 Type:" VerticalAlignment="Center" />
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <RadioButton Name="rbPlayer2Human" Content="Human" IsChecked="True" Margin="0,0,20,0" />
            <RadioButton Name="rbPlayer2Computer" Content="Computer" />
        </StackPanel>
 
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Name="btnOk" Click="btnOk_Click" IsDefault="True" MinWidth="80" Margin="0,0,10,0">OK</Button>
            <Button Name="btnCancel" IsCancel="True" MinWidth="80">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>